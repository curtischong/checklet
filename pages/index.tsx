import React from "react";

import { Container, AppNav, Main } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Main />
            <AppNav />
        </Container>
    );
};

export default Home;
