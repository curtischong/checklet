# this is a non-standardly-named env file. why? so we can properly load the env variables on build: https://github.com/vercel/next.js/discussions/14240
NEXT_PUBLIC_BASE_URL=http://localhost:3000/
NEXT_PUBLIC_IS_LOGGING_ENABLED=true