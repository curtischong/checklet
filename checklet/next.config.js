/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "standalone",
    // rules: [
    //     {
    //         test: /\.css$/i,
    //         use: ["style-loader", "css-loader"],
    //     },
    // ],
};

module.exports = nextConfig;
